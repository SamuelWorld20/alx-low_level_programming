#include "main.h"
#include <stdio.h>
/**
 * add - This function adds two integers.
 * @a: First paramenter argument.
 * @b: Second parameter argument.
 *
 * Return: Always 0.
 */
int add(int a, int b)
{
	int result = a + b;

	return (result);
}
