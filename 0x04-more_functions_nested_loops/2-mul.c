#include "main.h"
#include <stdio.h>
/**
 * mul - This function multiplies two integers.
 * @a: First parameter that collects first integer.
 * @b: Second parameter that collects second integer.
 *
 * Return: Always 0.
 */
int mul(int a, int b)
{
	return (a * b);
}
